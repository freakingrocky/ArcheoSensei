# RAG Stack Monorepo

- Frontend: Next.js (TS) in `/frontend`
- Backend: FastAPI in `/backend`
- Worker: Python ingestion in `/worker`

Contributor note: see `AGENTS.md` for repo-specific guidance and workflows.

## Dev (Docker)

```bash
cp .env.example .env
./scripts/dev-up.sh
```
